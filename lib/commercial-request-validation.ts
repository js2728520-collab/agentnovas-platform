import { ResearchApiError } from "./research-errors.ts";

function required(body:Record<string,unknown>,key:string,maximum:number){const value=typeof body[key]==="string"?body[key].trim():"";if(!value||value.length>maximum)throw new ResearchApiError("VALIDATION_ERROR",`${key} 无效`,422,{fields:[key]});return value;}
function optional(body:Record<string,unknown>,key:string,maximum:number){if(body[key]===undefined||body[key]===null||body[key]==="")return undefined;return required(body,key,maximum);}
export function idempotencyKey(request:Request){const value=request.headers.get("Idempotency-Key")?.trim()??"";if(!/^[A-Za-z0-9._:-]{8,128}$/.test(value))throw new ResearchApiError("IDEMPOTENCY_KEY_REQUIRED","必须提供有效的 Idempotency-Key 请求头",422,{fields:["Idempotency-Key"]});return value;}
export function paymentEvidenceInput(body:Record<string,unknown>,expectedCurrency:"USD"|"USDT"){const evidenceKind=required(body,"evidenceKind",40);if(!["bank_transfer","manual_invoice","provider_reference"].includes(evidenceKind))throw new ResearchApiError("VALIDATION_ERROR","evidenceKind 无效",422,{fields:["evidenceKind"]});const amount=required(body,"amount",50);if(!/^\d+(?:\.\d{1,18})?$/.test(amount)||Number(amount)<=0)throw new ResearchApiError("VALIDATION_ERROR","amount 必须是正数且最多 18 位小数",422,{fields:["amount"]});const currency=required(body,"currency",10).toUpperCase();if(currency!==expectedCurrency)throw new ResearchApiError("VALIDATION_ERROR",`currency 必须为 ${expectedCurrency}`,422,{fields:["currency"]});const occurredAt=required(body,"occurredAt",40),occurred=new Date(occurredAt);if(Number.isNaN(occurred.valueOf())||occurred.getTime()>Date.now()+300_000)throw new ResearchApiError("VALIDATION_ERROR","occurredAt 无效或晚于当前时间",422,{fields:["occurredAt"]});return {evidenceKind,providerLabel:optional(body,"providerLabel",80),reference:required(body,"reference",256),amount,currency,occurredAt:occurred.toISOString(),note:optional(body,"note",500)};}
