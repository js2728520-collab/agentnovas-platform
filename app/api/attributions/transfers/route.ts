import { eq } from "drizzle-orm";
import { getDb } from "@/db";
import { approvalRequests, customerAttributions } from "@/db/schema";
import { requireAccessPermission } from "@/lib/access-control";
import { canAccessOrganization } from "@/lib/operations-access";
import { responseError } from "@/lib/session";
export async function POST(request:Request){try{const {user:actor,scope,organizationIds}=await requireAccessPermission(request,"ops.attributions.manage");const body=await request.json() as {attributionId?:string,managerId?:string,supervisorId?:string,employeeId?:string,effectiveAt?:string,reason?:string};if(!body.attributionId||!body.managerId||!body.reason?.trim())return Response.json({error:"缺少客户、经理或转移原因"},{status:400});const db=getDb();const current=(await db.select().from(customerAttributions).where(eq(customerAttributions.id,body.attributionId)).limit(1))[0];if(!current?.branchId||!canAccessOrganization(scope,{userId:actor.id,organizationId:actor.organizationId},current.branchId,organizationIds))return Response.json({error:"客户不属于授权范围"},{status:403});const id=crypto.randomUUID();await db.insert(approvalRequests).values({id,type:"customer_transfer",branchId:current.branchId,subjectType:"customer_attribution",subjectId:current.id,payloadJson:JSON.stringify({branchId:current.branchId,managerId:body.managerId,supervisorId:body.supervisorId||null,employeeId:body.employeeId||null,effectiveAt:body.effectiveAt||new Date().toISOString(),reason:body.reason,previous:{managerId:current.managerId,supervisorId:current.supervisorId,employeeId:current.employeeId}}),requestedBy:actor.id});return Response.json({requestId:id,status:"pending",requiredApprovals:2},{status:201});}catch(e){return responseError(e)}}
