import { requireAccessPermission } from "@/lib/access-control";
import { commercialListInput } from "@/lib/commercial-api";
import { encodeCommercialCursor } from "@/lib/commercial-api-support";
import { getPostgresPool } from "@/lib/postgres";
import { researchErrorResponse } from "@/lib/research-api";
export async function GET(request:Request){try{const {user}=await requireAccessPermission(request,"client.wallet.view");const {limit,cursor}=commercialListInput(request);const params:unknown[]=[user.id];let c="";if(cursor){params.push(cursor.createdAt,cursor.id);c=`AND (created_at,id)<($2::timestamptz,$3)`;}params.push(limit+1);const pool=await getPostgresPool();const result=await pool.query(`SELECT id,week_start,week_end,week_net_pnl::text,eligible_profit::text,loss_carry::text,fee_bps,fee_amount::text,currency,status,created_at FROM performance_fee_statements WHERE user_id=$1 ${c} ORDER BY created_at DESC,id DESC LIMIT $${params.length}`,params);const rows=result.rows.slice(0,limit),last=rows.at(-1);return Response.json({statements:rows.map(r=>({id:r.id,weekStart:new Date(r.week_start).toISOString(),weekEnd:new Date(r.week_end).toISOString(),weekNetPnl:r.week_net_pnl,eligibleProfit:r.eligible_profit,lossCarry:r.loss_carry,feeBps:r.fee_bps,feeAmount:r.fee_amount,currency:r.currency,status:r.status,createdAt:new Date(r.created_at).toISOString()})),nextCursor:result.rows.length>limit&&last?encodeCommercialCursor({createdAt:new Date(last.created_at).toISOString(),id:last.id}):null},{headers:{"cache-control":"no-store"}});}catch(error){return researchErrorResponse(error);}}
