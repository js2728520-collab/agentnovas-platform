# AgentNovas 创始人待办清单与真实交易闭环接入指南

> `RETIRED`：这是 2026-08-13 的历史规划，不是当前商业 Beta 的实现或上线指令。当前边界以 `docs/product/PRD.md`、`docs/specs/SYSTEM_SPEC.md` 和 `docs/quality/ACCEPTANCE_AND_RELEASE_GATES.md` 为准；客户不提供交易所密钥，真实交易、Payment Worker 和自动支付继续关闭。

> 版本：V1.1（本地开发阶段，2026-08-13）  
> 目标：把当前产品从“可操作界面”推进到“真实数据、真实回测、模拟盘闭环、可审核上架”的系统。  
> 安全规则：任何 API Secret、私钥、邮箱密码、助记词都不要发到聊天、截图或普通文档中，只能直接写入 Linux 服务端受限环境文件、平台加密配置或密码管理器。

## 一、你现在最先要做的 6 件事

### 先看这里：今天按顺序完成

1. 立即轮换此前在聊天、截图或终端里显示过的管理员密码、初始化密钥和其他 Secret；旧值全部作废。
2. 办理一个可供服务器调用的 AI 模型 API，记录服务商名称、接口地址和模型 ID。
3. 创建 OKX Demo Trading API，只开启读取与交易权限，完成后在网站“交易中心”添加并点击联网检测。
4. 在密码管理器中生成并保存交易凭证加密密钥与自动任务密钥，两条必须不同。
5. 整理三个官方 AI 策略的非敏感规则表；不需要现在提交真正的策略代码。
6. 确定至少两名策略审核员，保证社区策略上架可执行双人复核。
7. 告诉我以上事项的“完成状态”和非敏感名称；API Key、Secret、Passphrase 和私钥不要发给我。

这些步骤完成前，网站仍可继续开发、真实回测和站内模拟成交；OKX Demo 真实下单开关保持关闭。

### 1. 选定 AI 服务商并取得 API

策略生成对话需要一个兼容 OpenAI Chat Completions 格式的模型接口。你需要准备：

- API 请求地址，例如服务商提供的 `.../v1/chat/completions`
- API Key
- 模型名称
- 账户已充值并允许服务器所在地区访问

历史示例曾使用共享环境文件；当前禁止采用。实际部署必须使用 `deploy/env/*.env.example` 对应的独立最小环境文件，权限不得高于 `0640`：

| 变量名 | 类型 | 内容 |
| --- | --- | --- |
| `AI_API_URL` | 普通变量 | 完整 Chat Completions 请求地址 |
| `AI_MODEL` | 普通变量 | 服务商给出的模型 ID |
| `AI_API_KEY` | 加密秘密 | API Key，不得公开 |

验收：在“我的策略 → AI 策略生成”中提问，响应模式应显示为真实 AI 服务，而不是规则引导降级模式。

### 2. 取得历史行情与实时行情数据源

第一阶段可先用 Binance 公开 K 线接口做历史回测；为了稳定运行，建议另备一个付费或自建行情代理。你需要决定：

- 首个真实行情源：Binance 或 OKX
- 是否允许 Linux 服务器出口 IP 直接访问
- 是否需要购买商业行情套餐
- 新闻数据供应商（可以晚一阶段办理）

后台变量：

| 变量名 | 类型 | 内容 |
| --- | --- | --- |
| `MARKET_DATA_BASE_URL` | 普通变量 | 兼容 Binance K 线格式的基础地址；不填则使用 Binance |
| `MARKET_DATA_PROVIDER` | 普通变量 | 展示在回测证据里的数据商名称 |

验收：回测结果必须包含数据商、起止时间、K 线数量、手续费、滑点、交易样本和证据哈希。

### 3. 申请交易所模拟盘 API（先做 OKX，再扩八家）

你需要创建公司专用测试账户，不要使用个人主账户。第一家建议 OKX Demo Trading，准备：

- API Key
- Secret Key
- Passphrase（OKX 必填）
- 读取权限
- 交易权限
- 测试账户或模拟盘标识
- 固定 IP 白名单要求（若交易所支持）

随后再依次准备 Binance、Bybit、Bitget、Gate.io、KuCoin、Coinbase、Kraken。

注意：交易 API 与提现结算凭证必须分开保存、分开服务、分开审批。即使业务允许客户授权提现，也不能让普通交易执行进程直接持有提现权限。

### 4. 生成交易所凭证加密主密钥

系统不会明文保存客户交易所 API。你需要在本机密码管理器生成并保存一条高强度随机密钥，然后写入 Linux 服务端环境文件：

| 变量名 | 类型 | 内容 |
| --- | --- | --- |
| `EXCHANGE_CREDENTIAL_ENCRYPTION_KEY` | 加密秘密 | 至少 32 字节随机值 |

该值丢失后，已经加密的交易所凭证将无法解密；泄露后必须轮换全部客户凭证。

### 5. 准备通知渠道账号

你需要办理：

- 公司发件邮箱和邮件服务商 API（注册验证、找回密码、账单催收）
- Telegram Bot 与官方客服用户名
- WhatsApp Business API 账号与官方号码

需要向服务商取得的资料：发送域名、API Key、Webhook Secret、发件人名称、模板审批结果。未取得前后台可继续排队记录，但不会真实送达。

### 6. 准备链上收款信息

会员费和人工账单首期需要：

- TRC20 收款地址
- ERC20 收款地址
- BEP20 收款地址
- 每条链的区块确认数
- 链上监听服务商或自建节点方案
- 财务人员与双人审批人名单

不要把助记词或私钥交给网站。网站只保存公开收款地址；付款监听与付款签名必须隔离。

## 二、请按这个顺序办理（可逐项勾选）

### A. 本周必须完成：模拟盘闭环所需资料

- [ ] 选定 AI 服务商，确认支持 Chat Completions 兼容接口。
- [ ] 在服务商后台完成充值，并确认 Linux 服务器出口 IP 可以访问。
- [ ] 记录 `AI_API_URL` 和 `AI_MODEL`；`AI_API_KEY` 只写入服务端受限环境文件或平台加密模型配置。
- [ ] 创建 OKX 公司测试账户，开通 Demo Trading API 的读取和交易权限。
- [ ] 确认 OKX Demo API 的 Key、Secret、Passphrase 可用；只写入加密变量，不发聊天。
- [ ] 决定首期行情使用 Binance 公开行情还是 OKX 行情。
- [ ] 生成两条不同的随机密钥：交易凭证加密主密钥、自动运行内部密钥。
- [ ] 提供三个官方 AI 策略的非敏感规则表，至少包括币种、周期、风格、仓位、止损、止盈、最大回撤和暂停条件。
- [ ] 确定至少两名策略审核员邮箱，以便验证双人审核。

你完成后只需要告诉我：服务商名称、模型 ID、OKX Demo 已开通、首个行情源、三套策略规则和审核员邮箱。所有 Secret 都不需要告诉我。

### B. 第二阶段办理：通知与收费

- [ ] 公司发件域名、发件邮箱和邮件 API。
- [ ] Telegram Bot、客服用户名和 Webhook Secret。
- [ ] WhatsApp Business 账号、号码和获批消息模板。
- [ ] 三条 USDT 收款链的公开地址和到账确认数。
- [ ] 链上交易监听服务商。
- [ ] 财务操作员与财务审核员名单。

### C. 实盘前才办理

- [ ] OKX 小额实盘专用账户和 API，不使用公司资产主账户。
- [ ] 每家交易所的固定 IP、速率限制、合约模式、持仓模式和最小下单量资料。
- [ ] 紧急联系人与事故处理值班表。
- [ ] 三个官方策略完成历史回测、压力测试、模拟盘和小额实盘验收。
- [ ] 确认禁止经营地区、风险披露和客户协议最终文本。

实盘开关在以上验收完成前保持关闭。仅仅填入 API Key 不等于可以安全开放实盘。

## 三、你可以后续办理的事项

以下不会阻塞第一阶段“真实回测＋模拟盘闭环”，可稍后完成：

- 新闻数据 API
- Telegram、WhatsApp 正式模板审批
- 七种语言的人工校对
- 三档会员具体差异化权益
- 八家交易所全部实盘 API
- 策略作者链上结算地址
- 阶梯式分红方案
- 真实小额实盘白名单

## 四、你需要整理给系统的业务资料（不包含任何密钥）

请另建一个表格，填写：

1. 公司名称、客服 Telegram、客服邮箱、WhatsApp 号码。
2. 三档会员每档可连接账户数、可运行策略数、Agent 报告权限。
3. 三个官方 AI 策略的名称、允许币种、周期、最大仓位、止损、最大回撤、暂停条件。
4. 策略审核员和审计员名单；每人邮箱和角色，不填密码。
5. 分公司管理员名单和组织归属。
6. 账单、催收、API 异常、开仓、平仓、熔断通知模板。
7. 支持地区、禁止地区和免责声明最终文本。

## 五、目前后台已经打通的闭环

### 策略创作闭环

`专业对话引导 → 结构化参数 → 保存版本 → 可选历史回测/模拟测试 → 提交人工审核 → 双人审核 → 上架策略广场`

关键约束：

- 用户不能自己填写收益、胜率、回撤来冒充平台回测。
- 每次修改策略都会产生新版本，旧版本的回测报告只作为历史资料，不复制到新版本。
- 回测必须由服务端拉取历史 K 线，并计入手续费与滑点。
- 模拟测试只统计带策略编号、决策编号且已经平仓的模拟订单。
- 回测与模拟测试均为作者可选的研究工具，不设置“通过/不通过”，也不阻止保存或提交审核。
- 人工审核根据策略内容、风险边界和披露完整度作出决定，不要求任何验证器记录。
- 审核通过前不能被其他客户跟随。

### 跟随与模拟执行闭环

`选择已上架策略 → 选择本人模拟账户 → 会员检查 → API 权限检查 → 风控检查 → 激活订阅 → 生成决策编号 → 模拟下单 → 成交同步 → 平仓 → 收益归因 → 审计归档`

关键约束：

- 只能选择属于当前客户且已经通过读取、交易权限检查的账户。
- 首阶段仅允许模拟盘，不能误入真实账户。
- 每笔订单保存策略 ID、策略版本、订阅 ID、决策 ID、账户 ID 和成交结果。
- 客户独立手动订单没有平台决策 ID，不进入平台收益和佣金统计。

自动运行由常驻 `Runtime Worker` 触发，不再暴露 HTTP 调度入口。Worker 从 PostgreSQL 领取租约，使用 fencing token、完整 K 线唯一键和订单意图幂等键防止重复处理。

每轮固定执行：

`完整K线 → DSL V3 确定性信号 → 反方异议 → 确定性风控 → 影子/模拟订单意图 → 资金费率与费用 → 七角色事件 → 审计记录`

安全约束：

- 只允许 `shadow` 或 `paper`，不向 OKX、Binance、Bybit 的任何真实或验证订单接口发送请求。
- 同一根完整 K 线只处理一次；信号在收盘确认，下一根开盘模拟成交。
- LLM 解释超时或未配置不会阻断确定性运行链，也无权修改信号、风控或订单。
- 平台紧急停止、会员失效或风险熔断时拒绝新开仓；已有模拟仓位继续按固定退出规则管理。
- 没有信号时保存观望证据，不生成虚构交易。

### 上架与作者收益闭环

`平台收到客户绩效分成 → 平台收入确认 → 50% 平台费用 → 50% 作者应得 → 每月锁定 → 双人财务审核 → 5 日人工付款`

没有实际收到客户分成时，不生成作者可结算收入，也不生成组织业绩。

## 六、第一阶段验收清单

以下全部打勾后，才叫完成“模拟盘闭环”：

- [ ] 邮箱注册、验证、登录、找回密码正常
- [ ] 超级管理员、审核员、客户三类账号可用
- [ ] PostgreSQL 数据库已执行最新迁移
- [ ] AI 对话使用真实模型接口；接口故障时明确显示降级模式
- [ ] 历史回测结果来自服务端真实 K 线
- [ ] 修改策略后旧回测报告仍可追溯，但不会自动套用到新版本
- [ ] 模拟交易账户连接和权限检查正常
- [ ] 所有真实与验证环境订单路由均保持关闭
- [ ] 策略可产生至少 5 笔带决策编号的模拟闭环订单
- [ ] 模拟测试统计与订单明细一致
- [ ] 未运行回测或模拟测试的策略也可以提交人工审核
- [ ] 策略提交后需要两名不同审核员批准
- [ ] 客户只能跟随已上架策略
- [ ] 跟随参数、风险同意、账户和会员限制被保存
- [ ] 紧急停止后禁止新开仓，但允许平仓和撤单
- [ ] 每笔动作有审计记录
- [ ] 页面不再把演示结果标成真实结果

### Runtime Worker 专项验收

- [ ] Linux 服务端已设置 `STRATEGY_RUNTIME_ENABLED=true` 并启动独立 Runtime Worker
- [ ] 旧 HTTP 自动入口固定返回 410
- [ ] 重复调用同一根K线不会生成重复订单
- [ ] 无入场信号时只保存观望证据
- [ ] 出现入场信号时生成决策编号、模拟订单和审计记录
- [ ] 止损、止盈或退出信号出现时能自动平仓
- [ ] 暂停跟随后不再产生新仓位
- [ ] 平台紧急停止后禁止新开仓，已有仓位仍可退出
- [ ] 会员到期或催收停开仓后不产生新仓位
- [ ] 交易中心显示的收益能追溯到策略、版本、账户和决策编号

## 七、上线前安全动作

1. 轮换曾经出现在聊天或截图中的 `BOOTSTRAP_SECRET` 和管理员密码。
2. 删除或锁定初始化超级管理员入口。
3. 为 Linux 服务器管理入口、GitHub、域名注册商启用双重验证（这是管理后台安全能力，不是客户端页面里的验证器功能）。
4. 检查 Git 仓库中没有 `.env`、私钥、交易所 Secret 或数据库导出文件。
5. 只在模拟盘验收完成后建立小额实盘白名单。
6. 全平台紧急停止开关必须由总公司技术安全人员保留。
7. 客户侧不启用 Google Authenticator/TOTP 验证器；保留邮箱验证、密码找回及敏感操作审批。服务器、GitHub、域名和公司邮箱等管理后台仍必须启用双重验证。

## 八、当前完成度与仍需开发的部分

### 已在本地代码中打通

- 邮箱、邀请码、组织层级和权限基础数据结构。
- 交易所凭证加密保存入口、账户权限记录和模拟/实盘环境隔离。
- 专业策略对话、结构化策略版本、真实历史K线回测。
- 可选回测、可选模拟测试、提交人工审核、双人审批和上架流程。
- 策略详情、跟随参数、会员限制和风险同意。
- 自动模拟信号、硬风控、模拟成交、平仓、收益归因和审计链。
- 策略暂停、恢复、停止，以及恢复时重新风控。
- 策略作者收益和组织收入的数据结构与业务规则。

### 等你取得资料后继续接通

- 正式 AI 模型 API；未配置时只能使用明确标记的规则引导降级模式。
- OKX Demo 的账户余额、交易规则/最小下单量、撤单及交易所原生持仓对账；模拟下单与成交回执同步已经接入。
- Binance、Bybit、Bitget、Gate.io、KuCoin、Coinbase、Kraken 七家适配器。
- 邮件、Telegram、WhatsApp 的真实发送服务。
- 链上会员支付监听与自动开通会员。
- 三个官方 AI 策略的正式规则、独立版本以及真实回测与实盘记录。
- 新闻数据和异常行情多数据源交叉验证。
- 小额实盘白名单和实盘订单执行器。

### 明确不能只靠“填 API”完成的事项

- 不同交易所签名、时间同步、持仓模式、最小下单量和订单状态机不同，必须逐家写适配器并测试。
- AI 不能越过硬风控直接下单；AI 输出必须结构化并经过确定性规则校验。
- 策略可以不经回测或模拟测试直接提交人工审核，但不能把页面演示数据当作真实业绩；缺少真实记录时必须明确显示“暂无真实记录”。
- 实盘前必须完成断线重连、幂等、滑点、部分成交、限频和紧急停止测试。

## 九、免费与可申请接口目录（已落地到后台）

管理员后台的“行情新闻 API”页面现在包含一份非敏感的接口目录，按“行情数据、新闻与事件、大模型、搜索与研究”分组，记录供应商用途、免费额度说明、申请/文档地址、所需环境变量和当前接入状态。目录中已登记 Binance 公共行情、CoinGecko、CoinCap、CryptoCompare、Twelve Data、CoinDesk RSS、Cointelegraph RSS、GDELT、CryptoPanic、NewsAPI、OpenAI 兼容接口、DeepSeek、Gemini、Claude、Tavily、Brave Search 和 SerpAPI。

当前真正接通并可被页面路由读取的是：

- Binance 公共 24 小时行情：`/api/market/ticker`
- CoinDesk 与 Cointelegraph RSS：`/api/market/news`
- OpenAI 兼容大模型变量：`AI_API_URL`、`AI_API_KEY`、`AI_MODEL`（供策略工作流使用）

其他供应商先以“可配置”或“目录预留”状态显示，申请到 Key 后再完成对应的服务端适配，不会把未完成的供应商伪装成实时数据源。所有 Key 只放 `.env` 或部署平台的加密变量中，不能写进前端代码。

## 十、交易所适配真实状态

八家交易所均已在账户连接界面登记，但官方签名、下单、撤单、成交/持仓同步和沙盒验收目前只有 OKX 具备服务端入口；其余七家仍显示为待接入。接入顺序按 Binance → Bybit → Bitget → Gate.io → KuCoin → Coinbase → Kraken，每一家都要通过沙盒验收后才允许打开对应实盘开关。

## 十一、你完成资料后的交付格式

你只需回复“已办理”，并提供不敏感信息：

- AI 服务商名称、接口格式、模型 ID（不提供 Key）
- 首家交易所及 Demo/模拟盘类型（不提供 Secret）
- 行情供应商名称
- 邮件、Telegram、WhatsApp 是否已开通
- 三个官方 AI 策略的业务参数
- 审核员与管理员邮箱列表

密钥全部由你本人直接填入 Linux 服务端受限环境文件或平台加密配置，我再指导你逐项验证，不需要把密钥发给我。
