# 一次性容器的 secret 挂载路径必须匹配执行器合同

## 事件

迁移重放执行器固定读取 `/run/secrets/postgres_password`，首次调用却把整个目录挂载到另一个路径，导致只读重放在读取 secret 前失败。

## 长期约束

- 调用一次性迁移、备份或角色执行器前，先读取其固定 secret 路径合同。
- 优先把单个 secret 文件绑定到执行器声明的精确目标，不把命名不一致的整个目录当作可替代方案。
- 验证命令只输出迁移计数、文件权限和角色能力，不输出 secret 值。
