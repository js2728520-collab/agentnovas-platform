# Auth

Shared authentication and authorization helpers for app-audience sessions and RBAC enforcement.

The current implementation lives in `lib/session.ts`, `lib/riverton-apps.ts`, and `lib/access-control.ts`.

