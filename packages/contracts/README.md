# Contracts

Shared public contracts for the client, operations, maintenance, and worker services.

This package currently re-exports the root `lib/` contracts so the migration can stay incremental while the three-app split stabilizes.

