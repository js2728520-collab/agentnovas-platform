# Disabled until T8.2d/G7 evidence and explicit operator activation.
RELEASE_TARGET_ENABLED=false
DATABASE_URL=postgresql://agentnovas_release_target_gateway:REPLACE_OUTSIDE_GIT@127.0.0.1:5432/agentnovas
RELEASE_TARGET_HOST=127.0.0.1
RELEASE_TARGET_PORT=3312
RELEASE_TARGET_JOURNAL_ROOT=/var/lib/agentnovas-release-target
RELEASE_TARGET_CONTROL_HOST=127.0.0.1
RELEASE_TARGET_CONTROL_PORT=3313
RELEASE_TARGET_RECEIPT_KEY_ID=replace-with-approved-key-id
RELEASE_TARGET_AUDITOR_URL=http://127.0.0.1:3316/internal/restricted-cicd/audit
# RELEASE_TARGET_AUDITOR_SHARED_SECRET_FILE is injected by the systemd credential contract.
# RELEASE_TARGET_HOST_IDENTITY_FILE is injected by the systemd credential contract.
