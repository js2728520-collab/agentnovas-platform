# RETIRED — DO NOT USE AS A PROCESS ENVIRONMENT FILE.
# Use deploy/env/client.env.example, operations.env.example,
# maintenance.env.example, notification.env.example, configuration-activation.env.example, demo.env.example,
# runtime.env.example, and migrator.env.example. Legacy Research remains disabled;
# Payment Worker has no production unit or database login.
#
# Proxy trust remains blank in every web-specific template until the operator
# confirms that the app is not directly reachable and is served solely through the controlled reverse proxy.
TRUST_PROXY_HOPS=
